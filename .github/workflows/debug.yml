name: Debugging helper

on:
  workflow_dispatch:

jobs:
  debug:
    runs-on: ubuntu-latest

    steps:
    - name: Get ElevenLabs voice ID
      env:
        VOICE_ID: ${{ secrets.ELEVENLABS_VOICE_ID }}
      run: printf '%s' "$VOICE_ID" | base64 -w0
